<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <script crossorigin="anonymous" src="https://polyfill.io/v3/polyfill.min.js?flags=gated&features=default"></script>
  <link href="https://unpkg.com/sanitize.css" rel="stylesheet" />
  <link href="https://unpkg.com/sanitize.css/typography.css" rel="stylesheet" />
  <link href="https://unpkg.com/sanitize.css/forms.css" rel="stylesheet" />
  <script src="https://unpkg.com/feather-icons"></script>
  <link href="{{ url_for('static', filename=styles) }}" rel="stylesheet" />

  <title>Image Stats</title>


</head>
<body>
  <div class="content">
    <h1>Placeholder Image Generator</h1>

    <p>
      Ever needed to create some placeholder images?
      Here's an easy way to do that.
    </p>

    <h2>Images Created: {{ count }}</h2>

    <div class="image-container">
      <img id="example-image" src="{{ img_url }}?{{ img_query }}" alt="Example image.">
      <code id="endpoint">{{ img_url }}?{{ img_query }}</code>
      <i
        id="copy-button"
        data-feather="copy"
        title="Copy to clipboard."
      ></i>
    </div>


    <form id="params-form" action="">
      <label for="width">Width</label>
      <input id="width" type="number" min="1" step="1" value="{{ width }}">

      <label for="height">Height</label>
      <input id="height" type="number" min="1" step="1" value="{{ height }}">

      <label for="bg">Background Color</label>
      <input id="bg" type="text" value="{{ bg_color }}" pattern="([a-fA-F0-9]{3})|([a-fA-F0-9]{6})">

      <label for="fg">Text Color</label>
      <input id="fg" type="text" value="{{ fg_color }}">

      <label for="fmt">Format</label>
      <select name="fmt" id="fmt">
        <option selected value="webp">webp</option>
        <option value="png">png</option>
        <option value="jpg">jpg</option>
      </select>

      <label for="font">Font</label>
      <select name="font" id="font">
        {% for value, name in font_names %}
        {% if value == "overpass" %}
        <option selected value="{{ value }}">{{ name }}</option>
        {% else %}
        <option value="{{ value }}">{{ name }}</option>
        {% endif %}
        {% endfor %}
      </select>

      <label for="imageText">Text</label>
      <input id="imageText" type="text" value="{{ text }}" maxlength="30">
    </form>


    <br><br>

    <h3>Routes:</h3>
    {% for rule in rules %}
    <code>{{ rule|e }}</code>
    <br><br>
    {% endfor %}

  </div>
  <script src="{{ url_for('static', filename=main_js) }}"></script>
</body>
</html>